{{!-- Shield Rating Display Partial
     Pure display — no logic, no mutations
     Reads: system.derived.shield.* (engine-computed)
--}}

<section class="summary-block sr-block {{#unless system.derived.shield.active}}inactive{{/unless}}">
  <header class="summary-header">
    <span class="header-label">Shield Rating</span>
  </header>

  {{#if system.derived.shield.max}}
    <div class="sr-content">
      <div class="sr-bar">
        <div class="sr-fill"
             style="width: {{percentage system.derived.shield.current system.derived.shield.max}}%;"></div>
      </div>

      <div class="sr-values">
        <span class="sr-current">SR {{system.derived.shield.current}}</span>
        <span class="sr-separator">/</span>
        <span class="sr-max">{{system.derived.shield.max}}</span>
      </div>

      {{#if system.derived.shield.source}}
        <div class="sr-source">
          {{system.derived.shield.source}}
        </div>
      {{/if}}
    </div>
  {{else}}
    <div class="sr-none">
      <em>None</em>
    </div>
  {{/if}}
</section>
