<div class="swse-lightsaber-construction">
  <!-- Hologram Scanline Overlay -->
  <div class="ls-scanline-overlay"></div>

  <!-- SVG Background Frame -->
  <div class="ls-frame">
    <img src="systems/foundryvtt-swse/assets/ui/customization/lightsaber-preview-panel.svg" alt="Lightsaber Preview" />
  </div>

  <form class="ls-form flexcol">
    <!-- Title Section -->
    <header class="ls-header">
      <h1>⚔️ Kyber Crystal Assembly</h1>
      <p class="subtitle">Attune to the Force and construct your lightsaber</p>
    </header>

    <!-- Interactive Overlay -->
    <div class="ls-overlay">

      <!-- CRYSTAL SECTION (PRIMARY) -->
      <section class="ls-crystal-section ls-panel">
        <h3>Kyber Crystal</h3>

        <div class="ls-crystal-display">
          <img src="systems/foundryvtt-swse/assets/ui/customization/kyber-crystal-outline.svg" class="ls-crystal-svg" alt="Kyber Crystal" />
        </div>

        <div class="ls-crystal-options ls-options">
          {{#each crystals}}
          <div class="ls-option-card crystal-option" data-crystal-id="{{this.id}}"
               {{#if (eq this.id ../selectedCrystal.id)}}data-selected="true"{{/if}}>
            <div class="option-name">{{this.name}}</div>
            <div class="option-rarity rarity-{{this.rarity}}">{{this.rarity}}</div>
            <div class="option-stats">
              <span class="stat-badge">DC {{this.buildDcModifier}}</span>
              <span class="stat-badge">{{this.cost}} cr</span>
            </div>
          </div>
          {{/each}}
        </div>
      </section>

      <!-- BLADE COLOR SECTION -->
      <section class="ls-blade-section ls-panel">
        <h3>Blade Resonance</h3>
        <p class="color-subtitle">Click the color picker</p>

        <div class="ls-blade-picker">
          <object
            type="image/svg+xml"
            data="systems/foundryvtt-swse/assets/ui/customization/lightsaber-blade-color-picker.svg"
            class="ls-blade-svg"
          ></object>
        </div>

        <div class="ls-color-grid">
          {{#each colorOptions}}
          <div class="ls-color-cell" data-color="{{this}}"
               style="--blade-color: {{lookup ../colorMap this}}"
               {{#if (eq this ../selectedBladeColor)}}data-selected="true"{{/if}}>
            <div class="ls-blade-preview"></div>
            <div class="ls-color-label">{{this}}</div>
          </div>
          {{/each}}
        </div>
      </section>

      <!-- HILT & MODIFICATIONS SECTION -->
      <section class="ls-hilt-section ls-panel">
        <h3>Hilt & Modifications</h3>

        <div class="ls-hilt-preview">
          <img src="systems/foundryvtt-swse/assets/ui/customization/lightsaber-hilt-classic.svg" alt="Hilt Preview" />
        </div>

        <div class="ls-mod-options">
          <!-- CHASSIS SELECTION -->
          <div class="ls-subsection">
            <h4>Chassis</h4>
            <div class="ls-options">
              {{#each chassis}}
              <div class="ls-option-card" data-chassis-id="{{this.id}}"
                   {{#if (eq this.id ../selectedChassis.id)}}data-selected="true"{{/if}}>
                <div class="option-name">{{this.name}}</div>
                <div class="option-stats">
                  <span class="stat-badge">DC {{this.baseBuildDc}}</span>
                  <span class="stat-badge">{{this.baseCost}} cr</span>
                </div>
              </div>
              {{/each}}
            </div>
          </div>

          <!-- ACCESSORIES -->
          <div class="ls-subsection">
            <h4>Enhancements</h4>
            <div class="ls-options">
              {{#each accessories}}
              <div class="ls-option-card accessory-option" data-accessory-id="{{this.id}}">
                <div class="option-name">{{this.name}}</div>
                <div class="option-rarity rarity-{{this.rarity}}">{{this.rarity}}</div>
                <div class="option-stats">
                  <span class="stat-badge">DC {{this.buildDcModifier}}</span>
                  <span class="stat-badge">{{this.cost}} cr</span>
                </div>
              </div>
              {{/each}}
            </div>
          </div>
        </div>
      </section>

    </div>

    <!-- Summary Section -->
    <div class="ls-panel panel-summary">
      <div class="summary-row">
        <span class="summary-label">Selected Blade</span>
        <span class="summary-value" style="color: {{bladeColorHex}}">
          {{selectedBladeColor}}
        </span>
      </div>
      <div class="summary-row">
        <span class="summary-label">Total Cost</span>
        <span class="summary-value">
          {{#if selectedChassis}}
          {{add selectedChassis.baseCost
                (or selectedCrystal.cost 0)
                (sum (map selectedAccessories "cost") 0)}} credits
          {{else}}
          — credits
          {{/if}}
        </span>
      </div>
    </div>

    <!-- Build Button -->
    <footer class="ls-footer">
      <button type="button" class="ls-build-button"
              {{#unless (and selectedChassis selectedCrystal)}}disabled{{/unless}}>
        <span class="button-glow"></span>
        Construct Lightsaber
      </button>
    </footer>
  </form>
</div>
