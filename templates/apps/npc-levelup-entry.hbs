<div class="swse npc-levelup-entry">
  <header class="swse-header">
    <h2>{{actorName}}</h2>
    <p>
      Current Level: <b>{{totalLevel}}</b> —
      Mode: <b>{{mode}}</b> — Profile: <b>{{kindLabel}}</b> — Authority: <b>{{sourceAuthorityLabel}}</b>
    </p>
    <p class="hint">
      NPC progression uses the inferred profile context. Nonheroic, Beast, Follower, and imported statblock NPCs keep their own progression/legal assumptions.
    </p>
  </header>

  <div class="swse-npc-levelup-status">
    <div class="swse-npc-progress-grid">
      <div class="swse-npc-progress-stat">
        <label class="swse-npc-progress-stat__label">Heroic Levels</label>
        <div class="swse-npc-progress-stat__value">{{heroicLevels}}</div>
      </div>

      <div class="swse-npc-progress-stat">
        <label class="swse-npc-progress-stat__label">Nonheroic Levels</label>
        <div class="swse-npc-progress-stat__value">{{nonheroicLevels}}</div>
      </div>

      <div class="swse-npc-progress-stat">
        <label class="swse-npc-progress-stat__label">Total Levels</label>
        <div class="swse-npc-progress-stat__value">{{totalLevel}}</div>
      </div>

      <div class="swse-npc-progress-stat">
        <label class="swse-npc-progress-stat__label">Class Count</label>
        <div class="swse-npc-progress-stat__value">{{classCount}}</div>
      </div>
    </div>

    {{#if classNames.length}}
      <div class="swse-npc-progress-classes">
        <div class="swse-npc-progress-classes__label">Classes</div>
        <div class="swse-npc-chip-row">
          {{#each classNames as |className|}}
            <span class="swse-npc-chip">{{className}}</span>
          {{/each}}
        </div>
      </div>
    {{/if}}

    {{#if hasMixedProgressionTracks}}
      <div class="swse-npc-callout swse-npc-callout--warning">
        <strong>Mixed-track NPC:</strong>
        This NPC uses both heroic and nonheroic advancement tracks.
      </div>
    {{/if}}
  </div>

  {{#if heroicBlocked}}
    <p class="warning"><strong>Heroic:</strong> Epic Override required to take Heroic levels beyond 20.</p>
  {{/if}}
  {{#if nonheroicBlocked}}
    <p class="warning"><strong>Nonheroic:</strong> Epic Override required to take Nonheroic levels beyond 20.</p>
  {{/if}}
  {{#if heroicAdvisory}}
    <p class="hint"><strong>Heroic Epic Advisory:</strong> Suggestions are informational only.</p>
  {{/if}}
  {{#if nonheroicAdvisory}}
    <p class="hint"><strong>Nonheroic Epic Advisory:</strong> Suggestions are informational only.</p>
  {{/if}}

  {{#unless isGM}}
    <p class="warning">GM only.</p>
  {{/unless}}

  {{#if isOwnerSynced}}
    <div class="swse-npc-callout swse-npc-callout--info">
      <strong>Owner Sync:</strong>
      This NPC is owner-synced. Manual level-up is disabled; update/sync from the owner or Allies workflow instead.
    </div>
  {{/if}}

  {{#if isBeast}}
    <div class="swse-npc-callout swse-npc-callout--info">
      <strong>Beast Progression:</strong>
      Nonheroic level-up routes through the Beast subtype adapter, suppressing talents/Force features and using Beast-specific restrictions.
    </div>
  {{/if}}

  {{#if isFollower}}
    <div class="swse-npc-callout swse-npc-callout--info">
      <strong>Follower Context:</strong>
      Followers should normally be advanced from their owner or Allies entry so owner-level scaling and entitlement data stay intact.
    </div>
  {{/if}}

  <div class="swse-grid">
    <section class="swse-card">
      <h3>Heroic Level</h3>
      <p class="hint">Level like a PC (class + talent/feat choices).</p>
      <button type="button" data-action="heroic" {{#unless isGM}}disabled{{/unless}} {{#unless canManualLevel}}disabled{{/unless}} {{#if heroicBlocked}}disabled{{/if}}>Heroic{{#if heroicBlocked}} (Epic Override required){{/if}}</button>
    </section>

    <section class="swse-card">
      <h3>Nonheroic Level</h3>
      <p class="hint">Auto: +1 Nonheroic level, HP gain, feat at 3rd/6th/…, +1 ability at 4th/8th/… (pure nonheroic).</p>
      <button type="button" data-action="nonheroic" {{#unless isGM}}disabled{{/unless}} {{#unless canManualLevel}}disabled{{/unless}} {{#if nonheroicBlocked}}disabled{{/if}}>Nonheroic{{#if nonheroicBlocked}} (Epic Override required){{/if}}</button>
    </section>
  </div>

  <footer class="swse-footer">
    {{#if hasSnapshot}}
      <button type="button" data-action="revert" {{#unless isGM}}disabled{{/unless}}>Revert to Statblock</button>
    {{/if}}
    <button type="button" data-action="cancel">Cancel</button>
  </footer>
</div>

