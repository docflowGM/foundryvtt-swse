<form class="houserules-form">
  <nav class="tabs" data-group="primary-tabs">
    <a class="item active" data-tab="training">Force Training</a>
    <a class="item" data-tab="talents">Force Talents</a>
    <a class="item" data-tab="access">Force Access</a>
    <a class="item" data-tab="points">Force Points</a>
  </nav>
  
  <section class="content">
    <div class="tab active" data-group="primary-tabs" data-tab="training">
      <div class="form-group">
        <label>Force Training Attribute</label>
        <select name="forceTrainingAttribute">
          <option value="wisdom" {{#if (eq forceTrainingAttribute "wisdom")}}selected{{/if}}>
            Wisdom (Standard) - Traditional Force connection
          </option>
          <option value="charisma" {{#if (eq forceTrainingAttribute "charisma")}}selected{{/if}}>
            Charisma - Force of personality and will
          </option>
        </select>
        <p class="notes">
          <strong>Wisdom:</strong> Standard SWSE rules. Force users with high Wisdom have deeper connection to the Force.<br>
          <strong>Charisma:</strong> Alternative rule. Force users project their will through personality and presence.
        </p>
      </div>
    </div>
    
    <div class="tab" data-group="primary-tabs" data-tab="talents">
      <div class="form-group">
        <label>Block/Deflect Talents</label>
        <select name="blockDeflectTalents">
          <option value="separate" {{#if (eq blockDeflectTalents "separate")}}selected{{/if}}>
            Separate Talents (Standard)
          </option>
          <option value="combined" {{#if (eq blockDeflectTalents "combined")}}selected{{/if}}>
            Combined as One Talent
          </option>
        </select>
        <p class="notes">
          <strong>Separate:</strong> Block (melee) and Deflect (ranged) are taken as separate talents.<br>
          <strong>Combined:</strong> Single talent grants both Block and Deflect abilities, reducing talent tax.
        </p>
      </div>
    </div>
    
    <div class="tab" data-group="primary-tabs" data-tab="access">
      <div class="form-group">
        <label>Force Sensitive Feat Restriction</label>
        <input type="checkbox" name="forceSensitiveJediOnly" {{#if forceSensitiveJediOnly}}checked{{/if}}/>
        <label>Restrict Force Sensitive to Jedi classes only</label>
        <p class="notes">
          When enabled, only characters with at least one level in a Jedi class can take the Force Sensitive feat.
          This prevents non-Jedi from dipping into Force powers.
        </p>
      </div>
    </div>
    
    <div class="tab" data-group="primary-tabs" data-tab="points">
      <div class="form-group">
        <label>Force Point Recovery</label>
        <select name="forcePointRecovery">
          <option value="level" {{#if (eq forcePointRecovery "level")}}selected{{/if}}>
            On Level Up (Standard)
          </option>
          <option value="extended" {{#if (eq forcePointRecovery "extended")}}selected{{/if}}>
            Extended Rest
          </option>
          <option value="session" {{#if (eq forcePointRecovery "session")}}selected{{/if}}>
            Each Session
          </option>
        </select>
      </div>
      
      <div class="form-group">
        <label>Dark Side Temptation</label>
        <select name="darkSideTemptation">
          <option value="strict" {{#if (eq darkSideTemptation "strict")}}selected{{/if}}>
            Strict RAW
          </option>
          <option value="lenient" {{#if (eq darkSideTemptation "lenient")}}selected{{/if}}>
            Lenient
          </option>
          <option value="narrative" {{#if (eq darkSideTemptation "narrative")}}selected{{/if}}>
            Narrative Only
          </option>
        </select>
      </div>

      <div class="form-group">
        <input type="checkbox" name="darkSidePowerIncreaseScore" {{#if darkSidePowerIncreaseScore}}checked{{/if}}/>
        <label>Dark Side Powers Auto-Increase Score</label>
        <p class="notes">
          When enabled, using a [Dark Side] power automatically increases the Dark Side Score by 1. When disabled, the Dark Side Score only increases through explicit temptation mechanics.
        </p>
      </div>
    </div>
  </section>
  
  <footer class="sheet-footer">
    <button type="submit"><i class="{{getIconClass 'save'}}"></i> Save Changes</button>
    <button type="button" class="reset-defaults"><i class="fas fa-rotate-left"></i> Reset to Defaults</button>
  </footer>
</form>