<div class="adopt-dialog">
  <p class="dialog-prompt">
    <strong>{{actorName}}</strong> ({{actorType}})
  </p>
  <p class="dialog-text">
    How would you like to handle this drop?
  </p>

  <div class="adopt-options">
    <button type="button" class="adopt-btn" data-action="add">
      <i class="fas fa-plus"></i> Add to Inventory/Hangar
    </button>
    <button type="button" class="adopt-btn adopt-btn--primary" data-action="adopt">
      <i class="fas fa-refresh"></i> Adopt Stat Block
    </button>
  </div>

  <div class="dialog-footer">
    <button type="button" class="adopt-btn adopt-btn--cancel" data-action="cancel">
      Cancel
    </button>
  </div>
</div>

<style>
.adopt-dialog {
  padding: 1.5rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.dialog-prompt {
  font-size: 1.2rem;
  font-weight: bold;
  margin: 0;
}

.dialog-text {
  margin: 0;
  color: #666;
}

.adopt-options {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.adopt-btn {
  padding: 0.75rem 1rem;
  border: 1px solid #999;
  background-color: #f0f0f0;
  border-radius: 4px;
  cursor: pointer;
  font-size: 1rem;
  text-align: left;
  transition: all 0.2s ease;
  display: flex;
  align-items: center;
  gap: 0.5rem;
}

.adopt-btn:hover {
  background-color: #e0e0e0;
  border-color: #666;
}

.adopt-btn--primary {
  background-color: #3d71b8;
  color: white;
  border-color: #2a4f88;
}

.adopt-btn--primary:hover {
  background-color: #2a4f88;
  border-color: #1a3f78;
}

.adopt-btn--cancel {
  opacity: 0.7;
  margin-top: 0.5rem;
}

.adopt-btn--cancel:hover {
  opacity: 1;
}

.dialog-footer {
  border-top: 1px solid #ddd;
  padding-top: 1rem;
  margin-top: 0.5rem;
}
</style>
