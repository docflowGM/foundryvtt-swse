{{!-- Starship Maneuver Selection Work Surface --}}
{{!-- CRITICAL: Uses stacking/duplicate selection model like Force Powers --}}

<div class="prog-starship-maneuver-browse swse-starship-selector swse-store-stage">
  {{#if maneuvers.length}}
    <div class="prog-starship-maneuver-grid" role="list">
      {{#each maneuvers}}
        <div class="prog-starship-maneuver-card
                    {{#if (arrayIncludes ../committedCounts @key)}}prog-starship-maneuver-card--selected{{/if}}
                    {{#if this.isSuggested}}prog-maneuver-card--suggested prog-maneuver-card--suggested-{{this.confidenceLevel}}{{/if}}"
             data-action="focus-item"
             data-item-id="{{this.id}}"
             data-recommended="{{#if this.isSuggested}}true{{/if}}"
             data-confidence-level="{{this.confidenceLevel}}"
             tabindex="0" role="option">

          <div class="prog-starship-maneuver-card__header">
            <h3 class="prog-starship-maneuver-card__name">
              {{this.name}}
              {{#if this.badgeLabel}}
                <span class="swse-option-card__badge swse-option-card__badge--recommended">{{this.badgeLabel}}</span>
              {{/if}}
            </h3>
            {{#if (lookup ../committedCounts this.id)}}
              <span class="prog-starship-maneuver-card__count-badge">
                ×{{lookup ../committedCounts this.id}}
              </span>
            {{/if}}
          </div>

          {{#if this.description}}
            <p class="prog-starship-maneuver-card__description">
              {{truncate this.description 100}}
            </p>
          {{/if}}

          {{#if (lookup ../committedCounts this.id)}}
            <div class="prog-starship-maneuver-card__state-hint">
              <i class="fas fa-star" aria-hidden="true"></i>
              Selected {{lookup ../committedCounts this.id}} {{plural (lookup ../committedCounts this.id) "use" "uses"}}
            </div>

            {{!-- Phase 8: Work-surface quantity controls (parity with force-power) --}}
            <div class="prog-starship-maneuver-card__quantity-inline">
              <button class="prog-quantity-btn prog-quantity-btn--decrement"
                      data-action="decrement-quantity"
                      data-item-id="{{this.id}}"
                      type="button"
                      aria-label="Remove one selection"
                      title="Remove one">
                <i class="fas fa-minus" aria-hidden="true"></i>
              </button>
              <span class="prog-quantity-display">{{lookup ../committedCounts this.id}}</span>
              <button class="prog-quantity-btn prog-quantity-btn--increment"
                      data-action="increment-quantity"
                      data-item-id="{{this.id}}"
                      type="button"
                      aria-label="Add one more selection"
                      title="Add one">
                <i class="fas fa-plus" aria-hidden="true"></i>
              </button>
            </div>
          {{else}}
            <div class="prog-starship-maneuver-card__state-hint prog-starship-maneuver-card__state-hint--available">
              <i class="fas fa-plus-circle" aria-hidden="true"></i>
              Available
            </div>
          {{/if}}
        </div>
      {{/each}}
    </div>
  {{else}}
    <div class="prog-starship-maneuver-browse__empty">
      <i class="fas fa-rocket" aria-hidden="true"></i>
      <p>No Starship Maneuvers available.</p>
    </div>
  {{/if}}
</div>
