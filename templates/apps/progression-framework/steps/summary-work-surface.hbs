{{#if (eq mode "levelup")}}
<div class="prog-summary-surface prog-summary-surface--levelup">
  <div class="prog-panel prog-panel--center prog-panel--summary-preview">
    <h3 class="prog-panel-title">Level-Up Review</h3>
    <p class="prog-panel-subtitle">Final dry-run checkpoint before this advancement is written to the actor.</p>

    {{#if levelupSummary.levelInfo}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--identity swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-id-card"></i> Advancement Packet</span></div>
      <div class="prog-summary-delta-grid">
        <div class="prog-summary-delta-row"><span>Character Level</span><strong>{{levelupSummary.levelInfo.currentLevel}} → {{levelupSummary.levelInfo.newLevel}}</strong></div>
        <div class="prog-summary-delta-row"><span>Training Class</span><strong>{{levelupSummary.levelInfo.className}}{{#if levelupSummary.levelInfo.classLevel}} {{levelupSummary.levelInfo.classLevel}}{{/if}}</strong></div>
        <div class="prog-summary-delta-row"><span>Transition</span><strong>{{levelupSummary.levelInfo.transitionKind}}</strong></div>
      </div>
    </div>
    {{/if}}


    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card prog-finalization-card">
      <div class="prog-section-header"><span><i class="fas fa-heart-pulse"></i> Hit Point Finalization</span></div>
      <div class="prog-finalization-status {{#if hpGainState.resolved}}is-resolved{{else}}is-required{{/if}}">
        <span>{{#if hpGainState.resolved}}Resolved{{else}}Required{{/if}}</span>
        <strong>{{#if hpGainState.resolved}}+{{hpGainState.gain}} HP{{else}}Roll or choose maximum HP{{/if}}</strong>
      </div>
      <div class="prog-summary-delta-grid">
        <div class="prog-summary-delta-row"><span>Formula</span><strong>{{hpGainState.formula}}</strong></div>
        <div class="prog-summary-delta-row"><span>Rule</span><strong>{{hpGainState.ruleLabel}}</strong></div>
        {{#if hpGainState.maximumGain}}<div class="prog-summary-delta-row"><span>Maximum Option</span><strong>+{{hpGainState.maximumGain}}</strong></div>{{/if}}
      </div>
      {{#if hpGainState.needsResolution}}
        {{#unless hpGainState.resolved}}
          <div class="prog-finalization-actions">
            <button type="button" class="prog-finalization-btn prog-finalization-btn--primary" data-action="roll-hp"><i class="fas fa-dice-d20"></i> Roll HP</button>
            <button type="button" class="prog-finalization-btn" data-action="use-max-hp"><i class="fas fa-arrow-up-short-wide"></i> Take Max</button>
          </div>
        {{/unless}}
      {{/if}}
    </div>

    {{#if levelupSummary.creditPreview}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card prog-finalization-card">
      <div class="prog-section-header"><span><i class="fas fa-coins"></i> Credits Preview</span></div>
      <div class="prog-summary-delta-grid">
        <div class="prog-summary-delta-row"><span>Current Credits</span><strong>{{levelupSummary.creditPreview.currentCredits}}</strong></div>
        <div class="prog-summary-delta-row"><span>Pending Credits</span><strong class="{{#if levelupSummary.creditPreview.hasChanges}}prog-number-positive{{else}}prog-number-zero{{/if}}">+{{levelupSummary.creditPreview.pendingDelta}}</strong></div>
        <div class="prog-summary-delta-row"><span>After Confirmation</span><strong>{{levelupSummary.creditPreview.finalCredits}}</strong></div>
      </div>
      {{#if levelupSummary.creditPreview.sources.length}}
        <div class="prog-credit-source-list">
          {{#each levelupSummary.creditPreview.sources}}
            <div class="prog-credit-source prog-credit-source--{{this.tone}}"><span>{{this.label}}</span><strong>+{{this.amount}}</strong></div>
          {{/each}}
        </div>
      {{else}}
        <div class="prog-placeholder">No credit changes from this level-up.</div>
      {{/if}}
    </div>
    {{/if}}

    {{#if levelupSummary.metadataChanges.length}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--identity swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-layer-group"></i> Class Progression</span></div>
      <div class="prog-items-list">
        {{#each levelupSummary.metadataChanges}}
          <div class="prog-item-entry"><i class="fas {{#if this.isNew}}fa-plus-circle{{else}}fa-arrow-up{{/if}}"></i> {{this.after}}</div>
        {{/each}}
      </div>
    </div>
    {{/if}}

    {{#if levelupSummary.statChanges.length}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-chart-line"></i> Derived Changes</span></div>
      <div class="prog-summary-delta-grid">
        {{#each levelupSummary.statChanges}}
          <div class="prog-summary-delta-row">
            <span>{{this.label}}</span>
            <strong>{{this.before}} → {{this.after}}</strong>
          </div>
        {{/each}}
      </div>
    </div>
    {{/if}}

    {{#if levelupSummary.attributeChanges.length}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-dumbbell"></i> Attribute Changes</span></div>
      <div class="prog-summary-delta-grid">
        {{#each levelupSummary.attributeChanges}}
          <div class="prog-summary-delta-row"><span>{{this.label}}</span><strong>{{this.before}} → {{this.after}}</strong></div>
        {{/each}}
      </div>
    </div>
    {{/if}}

    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-plus-circle"></i> New Choices</span></div>
      <div class="prog-summary-columns">
        <div>
          <h4>Feats</h4>
          {{#if levelupSummary.addedFeats.length}}
            {{#each levelupSummary.addedFeats}}<div class="prog-item-entry"><i class="fas fa-check-circle"></i> {{this}}</div>{{/each}}
          {{else}}<div class="prog-placeholder">None</div>{{/if}}
        </div>
        <div>
          <h4>Talents</h4>
          {{#if levelupSummary.addedTalents.length}}
            {{#each levelupSummary.addedTalents}}<div class="prog-item-entry"><i class="fas fa-check-circle"></i> {{this}}</div>{{/each}}
          {{else}}<div class="prog-placeholder">None</div>{{/if}}
        </div>
        <div>
          <h4>Skills</h4>
          {{#if levelupSummary.addedSkills.length}}
            {{#each levelupSummary.addedSkills}}<div class="prog-item-entry"><i class="fas fa-check-circle"></i> <span class="prog-skill-token">{{this}}</span></div>{{/each}}
          {{else}}<div class="prog-placeholder">None</div>{{/if}}
        </div>
        <div>
          <h4>Force</h4>
          {{#if levelupSummary.addedForcePowers.length}}
            {{#each levelupSummary.addedForcePowers}}<div class="prog-item-entry"><i class="fas fa-check-circle"></i> {{this}}</div>{{/each}}
          {{/if}}
          {{#if levelupSummary.addedForceTechniques.length}}
            {{#each levelupSummary.addedForceTechniques}}<div class="prog-item-entry"><i class="fas fa-check-circle"></i> {{this}}</div>{{/each}}
          {{/if}}
          {{#if levelupSummary.addedForceSecrets.length}}
            {{#each levelupSummary.addedForceSecrets}}<div class="prog-item-entry"><i class="fas fa-check-circle"></i> {{this}}</div>{{/each}}
          {{/if}}
          {{#unless levelupSummary.addedForcePowers.length}}{{#unless levelupSummary.addedForceTechniques.length}}{{#unless levelupSummary.addedForceSecrets.length}}<div class="prog-placeholder">None</div>{{/unless}}{{/unless}}{{/unless}}
        </div>
        <div>
          <h4>Other Training</h4>
          {{#if levelupSummary.addedMedicalSecrets.length}}
            {{#each levelupSummary.addedMedicalSecrets}}<div class="prog-item-entry"><i class="fas fa-kit-medical"></i> {{this}}</div>{{/each}}
          {{/if}}
          {{#if levelupSummary.addedStarshipManeuvers.length}}
            {{#each levelupSummary.addedStarshipManeuvers}}<div class="prog-item-entry"><i class="fas fa-rocket"></i> {{this}}</div>{{/each}}
          {{/if}}
          {{#if levelupSummary.addedLanguages.length}}
            {{#each levelupSummary.addedLanguages}}<div class="prog-item-entry"><i class="fas fa-language"></i> {{this}}</div>{{/each}}
          {{/if}}
          {{#unless levelupSummary.addedMedicalSecrets.length}}{{#unless levelupSummary.addedStarshipManeuvers.length}}{{#unless levelupSummary.addedLanguages.length}}<div class="prog-placeholder">None</div>{{/unless}}{{/unless}}{{/unless}}
        </div>
      </div>
    </div>

    {{#if levelupSummary.mutationPreview}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-clipboard-check"></i> Mutation Dry Run</span></div>
      {{#unless levelupSummary.mutationPreview.success}}
        <div class="prog-placeholder prog-placeholder--warning">{{levelupSummary.mutationPreview.error}}</div>
      {{else}}
        {{#if levelupSummary.mutationPreview.validationErrors.length}}
          {{#each levelupSummary.mutationPreview.validationErrors}}<div class="prog-placeholder prog-placeholder--warning"><i class="fas fa-triangle-exclamation"></i> {{this}}</div>{{/each}}
        {{/if}}
        {{#if levelupSummary.mutationPreview.validationWarnings.length}}
          {{#each levelupSummary.mutationPreview.validationWarnings}}<div class="prog-placeholder"><i class="fas fa-circle-info"></i> {{this}}</div>{{/each}}
        {{/if}}
        <div class="prog-summary-columns">
          <div>
            <h4>Actor Fields</h4>
            {{#if levelupSummary.mutationPreview.setFields.length}}
              {{#each levelupSummary.mutationPreview.setFields}}<div class="prog-item-entry"><i class="fas fa-pen"></i> {{this.label}}: {{this.value}}</div>{{/each}}
            {{else}}<div class="prog-placeholder">None</div>{{/if}}
          </div>
          <div>
            <h4>New Items</h4>
            {{#if levelupSummary.mutationPreview.addedItems.length}}
              {{#each levelupSummary.mutationPreview.addedItems}}<div class="prog-item-entry"><i class="fas fa-plus"></i> {{this.name}} <small>({{this.type}})</small></div>{{/each}}
            {{else}}<div class="prog-placeholder">None</div>{{/if}}
          </div>
          <div>
            <h4>Updated Items</h4>
            {{#if levelupSummary.mutationPreview.updatedItems.length}}
              {{#each levelupSummary.mutationPreview.updatedItems}}<div class="prog-item-entry"><i class="fas fa-arrow-up"></i> {{this.label}}</div>{{/each}}
            {{else}}<div class="prog-placeholder">None</div>{{/if}}
          </div>
        </div>
      {{/unless}}
    </div>
    {{/if}}
  </div>
</div>
{{else}}
<div class="prog-summary-surface prog-summary-surface--chargen">
  <div class="prog-panel prog-panel--center prog-panel--summary-preview">
    <h3 class="prog-panel-title">Character Preview</h3>


    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card prog-finalization-card">
      <div class="prog-section-header"><span><i class="fas fa-clipboard-check"></i> Finalization Required</span></div>
      <div class="prog-finalization-grid">
        <div class="prog-finalization-mini-card is-resolved">
          <span class="prog-finalization-mini-card__label"><i class="fas fa-heart-pulse"></i> Starting HP</span>
          <strong>{{summary.hpCalculation.total}}</strong>
          <small>{{summary.hpCalculation.formula}}</small>
        </div>
        {{#if summary.creditsState}}
        <div class="prog-finalization-mini-card {{#if summary.creditsState.resolved}}is-resolved{{else}}is-required{{/if}}">
          <span class="prog-finalization-mini-card__label"><i class="fas fa-coins"></i> Starting Credits</span>
          <strong>{{#if summary.creditsState.resolved}}{{summary.creditsState.amount}}{{else}}Roll Required{{/if}}</strong>
          <small>{{summary.creditsState.ruleLabel}}{{#if summary.creditsState.formula}} · {{summary.creditsState.formula}}{{/if}}</small>
        </div>
        {{/if}}
      </div>
      {{#if summary.creditsState}}
        {{#if summary.creditsState.needsResolution}}
          {{#unless summary.creditsState.resolved}}
            <div class="prog-finalization-actions">
              <button type="button" class="prog-finalization-btn prog-finalization-btn--primary" data-action="roll-credits"><i class="fas fa-dice-d20"></i> Roll Starting Credits</button>
              <button type="button" class="prog-finalization-btn" data-action="use-max-credits"><i class="fas fa-arrow-up-short-wide"></i> Take Max</button>
            </div>
          {{/unless}}
        {{/if}}
        {{#if summary.creditsState.sources.length}}
          <div class="prog-credit-source-list">
            {{#each summary.creditsState.sources}}
              <div class="prog-credit-source prog-credit-source--{{this.tone}}"><span>{{this.label}}</span><strong>+{{this.amount}}</strong></div>
            {{/each}}
          </div>
        {{/if}}
      {{/if}}
    </div>

    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-fist-raised"></i> Derived Combat Stats</span></div>
      <div class="prog-combat-grid">
        {{#each summary.combatStats}}
          <div class="prog-combat-stat">
            <span class="prog-combat-stat__label">{{this.label}}</span>
            <span class="prog-combat-stat__value {{this.toneClass}}">{{this.value}}</span>
          </div>
        {{/each}}
      </div>
    </div>

    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header">
        <span><i class="fas fa-chart-bar"></i> Attributes</span>
        <button class="summary-step-edit-btn prog-inline-edit-btn" data-step="attributes" type="button" aria-label="Edit attributes"><i class="fas fa-pencil-alt"></i></button>
      </div>
      <div class="prog-attributes-grid prog-attributes-grid--summary">
        {{#each summary.attributeSummary}}
          <div class="prog-attr-display prog-attr-display--summary">
            <span class="prog-attr-name swse-ability-label swse-ability-label--{{this.key}}" data-ability="{{this.key}}">{{this.label}}</span>
            <span class="prog-attr-score">{{this.score}}</span>
            <span class="prog-attr-modifier {{this.toneClass}}">{{this.modifierDisplay}}</span>
          </div>
        {{/each}}
      </div>
    </div>

    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header">
        <span><i class="fas fa-book-open"></i> Skills</span>
        <button class="summary-step-edit-btn prog-inline-edit-btn" data-step="skills" type="button" aria-label="Edit skills"><i class="fas fa-pencil-alt"></i></button>
      </div>
      <div class="prog-skill-table">
        {{#if summary.skillRows.length}}
          {{#each summary.skillRows}}
            <div class="prog-skill-row {{#if this.trained}}is-trained{{/if}}">
              <span class="prog-skill-row__name prog-skill-token {{this.abilityClass}}" data-ability="{{this.abilityKey}}">{{this.label}}</span>
              <span class="prog-skill-row__meta {{this.abilityClass}}" data-ability="{{this.abilityKey}}">{{this.ability}}{{#if this.trained}} • Trained{{/if}}{{#if this.focused}} • Focus{{/if}}</span>
              <span class="prog-skill-row__total {{this.toneClass}}">{{this.totalDisplay}}</span>
            </div>
          {{/each}}
        {{else}}
          <div class="prog-placeholder">No trained skills selected</div>
        {{/if}}
      </div>
    </div>

    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header">
        <span><i class="fas fa-star"></i> Feats</span>
        <button class="summary-step-edit-btn prog-inline-edit-btn" data-step="general-feat" type="button" aria-label="Edit feats"><i class="fas fa-pencil-alt"></i></button>
      </div>
      <div class="prog-chip-list">
        {{#if summary.feats.length}}
          {{#each summary.feats}}<span class="prog-chip">{{this}}</span>{{/each}}
        {{else}}<span class="prog-placeholder">No feats selected</span>{{/if}}
      </div>
    </div>

    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header">
        <span><i class="fas fa-gem"></i> Talents</span>
        <button class="summary-step-edit-btn prog-inline-edit-btn" data-step="class-talent" type="button" aria-label="Edit talents"><i class="fas fa-pencil-alt"></i></button>
      </div>
      <div class="prog-chip-list">
        {{#if summary.talents.length}}
          {{#each summary.talents}}<span class="prog-chip">{{this}}</span>{{/each}}
        {{else}}<span class="prog-placeholder">No talents selected</span>{{/if}}
      </div>
    </div>

    {{#if summary.languages.length}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header">
        <span><i class="fas fa-language"></i> Languages</span>
        <button class="summary-step-edit-btn prog-inline-edit-btn" data-step="languages" type="button" aria-label="Edit languages"><i class="fas fa-pencil-alt"></i></button>
      </div>
      <div class="prog-chip-list">
        {{#each summary.languages}}<span class="prog-chip">{{this}}</span>{{/each}}
      </div>
    </div>
    {{/if}}

    {{#if summary.forcePowers.length}}
    <div class="prog-summary-section swse-card-schema swse-card-schema--enumerated swse-card-schema--filled prog-summary-card">
      <div class="prog-section-header"><span><i class="fas fa-hand-sparkles"></i> Force Powers</span></div>
      <div class="prog-chip-list">
        {{#each summary.forcePowers}}<span class="prog-chip">{{this}}</span>{{/each}}
      </div>
    </div>
    {{/if}}
  </div>
</div>
{{/if}}
