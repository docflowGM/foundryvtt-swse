{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Feat",
  "description": "Schema for Saga Edition feats",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the feat"
    },
    "type": {
      "type": "string",
      "description": "The category of the feat (e.g., General, Force, Combat)"
    },
    "prerequisite": {
      "type": ["string", "null"],
      "description": "The prerequisites needed to take the feat"
    },
    "description": {
      "type": "string",
      "description": "The rules text for the feat"
    },
    "sourcebook": {
      "type": "string",
      "description": "The book the feat is from"
    },
    "page": {
      "type": "integer",
      "description": "Page number where the feat is found"
    },
    "tags": {
      "type": "array",
      "description": "Keywords to help categorize the feat",
      "items": {
        "type": "string"
      }
    },
    "bonus_feat_for": {
      "type": "array",
      "description": "Which classes receive this as a bonus feat option",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["name", "type", "description", "sourcebook", "page", "tags", "bonus_feat_for"],
  "additionalProperties": false
}

