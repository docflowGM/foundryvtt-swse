# V2 Remaining-Work Ledger

**Purpose:** a concise, living project ledger — not a forensic history document. It
tracks what is actually proven in current `main`, what remains, and where the
evidence lives. Historical `docs/audits/*` files are leads, not truth; every
row below was re-verified against code on this branch's base commit.

**Base commit:** `98f1d73` (main, PR #973 merged)
**Branch:** `claude/v2-combat-runtime-convergence`
**This round's ACTIVE domains:** Damage packet/mitigation, Shields + DR, Action
Authority production wiring, Roll Expression/Transformation production
integration. All other rows are backlog context only — not touched in this PR.

| Domain | Current Authority | Proven Complete | Remaining Work | Evidence | Priority | Status |
|---|---|---|---|---|---|---|
| Damage generation | `resolveDamageBonus()` → `resolveDamageComposition()` → `buildDamageFormula()` (`scripts/engine/combat/combat-roll-math.js`) | Certified SSOT. Not reopened this round. | None in scope. | v2-math-integrity-authority-ledger.md | — | Certified (out of scope) |
| Damage packet | `buildCanonicalDamagePacket()` — `scripts/engine/combat/canonical-damage-packet.js:71-117`, chokepoint at `actor-engine.js:1371-1386` | Canonical `{components,amount,primaryType}` shape exists and is the single normalization chokepoint inside `ActorEngine.applyDamage`. Ordinary weapon Apply-Damage button and targeted `rollAndApplyDamage` both reach it (previously bare-number, now fixed). **`DarkSidePowers.js`'s 3 call sites (Wrath, Channel Aggression, Affliction) now pass explicit `type:'force'`** — fixed this round; they all route through `actor.applyDamage`→`ActorEngine.applyDamage`→the canonical packet, they just silently defaulted to `'normal'` before. | Remaining bare callers are intentional, not bugs: `/damage` chat command (`chat-commands.js:71`), GM manual Damage App (`damage-app.js:102-106`), `DamageSystem.applyToSelected` (`damage-system.js:40-52,171`) all confirmed to route through `actor.applyDamage`→canonical packet already, and legitimately have no type to carry (GM manual entry). `rollAndApplyDamage`'s no-target fallback (`damage.js:266-268`) can never mutate HP anyway (its own actor guard bails with no target). `CombatEngine.resolveAttack()` (`CombatEngine.js:311`) is effectively dead/unwired (only reachable via legacy combat-action-browser). | `scripts/talents/DarkSidePowers.js` (fixed), `tests/dark-side-powers-damage-type-authority.test.mjs`, `tests/canonical-damage-packet-authority.test.mjs` | ACTIVE (Phase 1) | Proven complete for all live callers |
| Damage mitigation | SR → Immunity (D4A) → DR w/ exceptions (D3) → Typed Resistance (D4) → Temp HP → HP, both single- (`damage-mitigation-manager.js:62-325`) and multi-component (`damage-component-mitigation.js:229-446`) | Full RAW order implemented and correct for both paths. Ion/Stun half-HP + pre-halving threshold + Evasion/Improved Evasion wired (`damage-packet-rules.js:191-368`, consumed by `damage-resolution-engine.js:241-245,285-289`). | Per-component (mixed-type) path is **dead code in practice**: `resolveComponentMitigation()` only activates at `components.length > 1`, but zero weapons/powers/items in `packs/*.db` ever produce >1 component (Dragite Crystal sonic-on-crit is an unconsumed `CRITICAL_BONUS` modifier). **Deliberately not fixed this round**: wiring Dragite Crystal's second component would require reopening `buildDamageFormula()`/`resolveDamageComposition()` inside the certified Damage-generation SSOT, which this round must not reopen. Documented as the adjudication boundary rather than patched around it. Physical subtypes (piercing/slashing/bludgeoning) still collapse into `kinetic` (`normalizeDamageTypeKey`, `damage-type-rules.js:35-38`). | `damage-component-rules.js:144-172`, `damage-type-rules.js:35-38,147-158,197` | ACTIVE (Phase 1) | Engine complete, unreachable from content (by design boundary) |
| Shields (SR) | `system.shields` → `DerivedCalculator` (`derived-calculator.js:946-982`) → `derived.shield` → `ShieldMitigationResolver` (pure read) → `ActorEngine.applyDamage` (`actor-engine.js:1402-1411`, `stored===true` guard) | Full path confirmed end-to-end: projection, mitigation, persistence guard against phantom writes, recharge primitive `ActorEngine.rechargeShields()` (`actor-engine.js:1485-1508`, +5 capped at max). | None — D1/D2 fully matches design and is current. | See file:line above | ACTIVE (Phase 2) | Proven complete |
| Damage Reduction | `DamageReductionResolver.getCanonicalDamageReductionEntries`/`getCurrentDR` (`damage-reduction-resolver.js:280-333`) reads 3 raw sources directly (derived.damageReduction if present, base `system.damageReduction`, item `abilityMeta.rules`) | Entries/exceptions collection and lightsaber DR-bypass (tag-based, `:164-168,358-363`) implemented and functional — D3's own "implemented" claim holds. | `DerivedCalculator` never produces `system.derived.damageReduction` (zero matches) — resolver remains a fragmented multi-source read, not the single canonical-projection design D3/phase-3b describe. Typed/`byType` DR axis (D4) untouched. | `derived-calculator.js` (no `damageReduction` writes), `damage-reduction-resolver.js:300-333` | ACTIVE (Phase 2) | Partially proven, functional but not canonical-projection shaped |
| Force Shield | `_buildForceShieldEffect` (`force-power-effects-engine.js:256-287`) writes an ActiveEffect directly overriding `system.derived.shield.current` | N/A | Still NOT integrated into canonical `system.shields`/`DerivedCalculator` pipeline — writes a parallel derived value via AE override rather than contributing a shield source. `stored` guard happens to prevent persistent-SR phantom writes as a side effect, but the design (source→DerivedCalculator→derived) is unrealized; concurrent stored-shield-plus-Force-Shield case has an unresolved override conflict (`base-actor.js:152-164`). | `force-power-effects-engine.js:256-287`, `base-actor.js:57-179` | ACTIVE (Phase 2) | Unintegrated, confirmed risk still live |
| Recharge/Restore Shields | Primitive: `ActorEngine.rechargeShields(actor,{amount=5})` (`actor-engine.js:1485-1508`) | Primitive itself proven correct (+5 capped at max). | **Zero call sites** to the primitive outside its own definition. `mechanics.recharge-shields`/`endurance.restore-shields` (`extra-skill-use-registry.js:45,47`) are display-only data rows; the actual skill-use flow (`character-like-sheet.js:6448-6517` → `skill-use-filter.js:249-298` → `rollSkillCheck`) never calls `ActorEngine.rechargeShields` after a successful roll. Needs full dispatch wiring. | See file:line above | ACTIVE (Phase 2) | Not wired |
| Action Authority (groundwork) | `ActionAvailabilityEngine` (`scripts/engine/actions/action-availability-engine.js:371-447`), `ActionDefinition`/`ActionEntitlement` (`action-definition.js:96-102`), `ActorActionResolver` (`actor-action-resolver.js:101`), `ActionRegistry` (`action-registry.js:65`) | 136-record (feats.db 88 + talents.db 48) strict discovery/normalization proven with 0 losses (`docs/audits/generated/action-authority-normalization-audit-report.md`). Tri-state availability, causal blocker propagation, fail-closed predicates all exercised by `tests/action-authority-groundwork*.test.mjs`. | **Zero production callers.** The module's own header states it is not wired into the live attack dialog. No golden reconciliation test vs. legacy presentation exists (Phase 3D is greenfield). | `action-availability-engine.js:2-23`, grep confirms 0 non-test importers | ACTIVE (Phase 3) | Groundwork proven, production wiring not begun |
| ATTACK_OPTION presentation (live) | `CombatOptionResolver` (`combat-option-resolver.js:257`) — owns BOTH mechanics (`collectAttackModifiers()` at `:330`, called from `attacks.js:316,670` and `combat-roll-math.js:427,692,733`, must not touch) AND discovery/presentation (`getAvailableAttackOptions`, `getAttackOptionsWithState`, `getAvailableAttackContexts`, `optionAllowedForWeapon`, `hydrateOption`) | Mechanics interpretation is correct and certified; do not touch. | Presentation collapses the richer tri-state model: maneuver/opportunity-attack/swift-action-gated options are excluded from the list entirely rather than shown as `disabled`/`external-workflow`/`unsupported` with a reason (`combat-option-resolver.js:234-238`). Live dialog built at `roll-config.js:1082,1091,1464`. | See file:line above | ACTIVE (Phase 3) | Live authority confirmed, presentation fidelity gap confirmed |
| Roll Formula Terms | `STATIC_MODIFIER`/`FORMULA_TERM`/`BASE_ROLL_TRANSFORM`/`RESULT_TRANSFORM` (`scripts/engine/roll/expression/roll-contribution-types.js:24-63`), `resolveFormulaContributions()`, `composeRoll()` (`compose-roll.js:19-43`) | Contract layer correct; `r`/`rr` confirmed (no invented `ro`); numeric `Modifier.value` untouched. | `composeRoll`/`resolveRollTransforms`/`applyRollTransforms` have **zero callers** in Attack (`attacks.js:361,691` still plain string concat) or Damage (`buildDamageFormula`, `combat-roll-math.js:1099-1134`, bespoke string-join) composition. Sneak Attack's FORMULA_TERM adapter (`damage-talent-contributions.js:74`) exists but is uncalled dead code; `resolveTalentDamageContributions()` (bespoke) is still what's live. | See file:line above | ACTIVE (Phase 4) | Infra proven, 0 real mechanics wired except Force Point |
| Roll Transforms | Same module; only live production caller is Force Point keep-highest (`force-point-spend-coordinator.js:62-99`, validate-before-spend confirmed at `:94-96`) | Force Point migration proven correct end-to-end. Take 10/20 suppression correct (`roll-transform-resolver.js:138-143`); `RollCore._handleTakeX()` never calls `Roll.evaluate()` — no early randomness found. | Real reroll/transform mechanics NOT on new authority: species reroll (bespoke re-execution), houserule exploding crit (hand-built regex, known unfixed `.evaluate()` bug) and "maxplus" reroll (bespoke `baseRoll.reroll()`), Knack talent and Embrace the Dark Side (both mechanically unimplemented stubs — flag/message only, no reroll executed by any authority), Force Scoundrel MISS_REROLL_RESOURCE (descriptor only). | See file:line above | ACTIVE (Phase 4) | Infra proven, migration of real mechanics not begun |
| Effective Treat-as-Light | — | Not investigated this round. | Backlog. | — | Backlog | Not investigated |
| Force Powers (general, non-shield/damage) | — | Not investigated this round. | Backlog. | — | Backlog | Not investigated |
| Vehicle runtime verification | — | Not investigated this round. | Backlog. | — | Backlog | Not investigated |
| Progression runtime verification | — | Not investigated this round. | Backlog. | — | Backlog | Not investigated |
| Prerequisite V2 migration | — | Not investigated this round. | Backlog. | — | Backlog | Not investigated |
| Combat feat/talent automation | — | Not investigated this round; some findings surfaced incidentally above (Knack, Embrace the Dark Side, Force Scoundrel are unimplemented stubs). | Backlog, except where it blocks an ACTIVE domain. | See Roll Transforms row | Backlog | Partially surfaced, not addressed |
| Holopad Games runtime QA | — | Not investigated this round. | Backlog. | — | Backlog | Not investigated |

## Stale/superseded historical audit claims identified this round

- `docs/audits/phase-3-component-damage-pipeline-design.md` and `phase-3b-shield-dr-design.md`'s "DR remains design-only" claim is **stale** — superseded by `phase-3-d3-damage-reduction-design.md`/`phase-3-d4-typed-mitigation-design.md`/`phase-3-d4a-immunity-design.md`, all "✅ Implemented" and verified current.
- `docs/audits/combat-phase-0d-damage-rules-fidelity-audit.md`, `combat-phase-0f-ion-damage-addendum.md`, `combat-phase-0g-damage-types-hazards-semantics-audit.md` claims about Ion/Stun and Evasion untrustworthiness and damage buttons losing attack-option context are **stale**, superseded by `combat-phase-2a/2b` implementation and the certified attack→damage option-context transport.
- `docs/audits/v2-damage-modifier-authority-audit.md §2`'s claim that `rollAndApplyDamage` drops type/components is **stale for the targeted case** (fixed) but **still accurate** for the untargeted fallback and `DarkSidePowers.js`.
- `docs/audits/combat-phase-0b-combat-action-routing-audit.md`, `combat-phase-1d-action-economy-hardening.md`, `combat-phase-1f-workflow-routing-and-damage-handoff.md` predate the Action Authority groundwork entirely (zero mentions of ActionAvailabilityEngine/136 records) — not authoritative for the current Action Authority domain state.
- Phase-0g's "physical subtypes (piercing/slashing/bludgeoning) collapse into kinetic, no canonical type registry" concern is **still accurate**, not stale.

## Not touched in this PR (explicitly out of scope per task instructions)

Prerequisite V2 migration, progression overhaul, vehicle crew UI, Holopad Games,
full feat backlog, full Force power implementation, Effect Intent redesign,
ModifierEngine replacement, CombatOptionResolver mechanical rewrite, Action
Economy architecture changes, opportunistic legacy pruning.
