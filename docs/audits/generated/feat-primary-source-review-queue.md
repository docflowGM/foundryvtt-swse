# Feat Primary-Source Review Queue

Hand-triaged from `docs/audits/generated/feat-prerequisite-input-report.json`'s `catalog_authority_text_disagreement` findings (30 total). This session has no access to the SWSE sourcebook PDFs, so these are **questions for a future pass that does have book access**, not resolved answers — nothing below has been checked against a primary source in this session.

Most of the 30 raw findings are cosmetic (clause reordering, a trailing period, "Droid, X" vs "Droid with X", or the catalog text carrying a spurious trailing "Feat" word already tracked separately as `feat_reference_has_spurious_suffix`) and are not included here — they don't require a book to resolve, since both sides plainly mean the same thing. The three below are genuine content disagreements: either a wholly different prerequisite, or an entire extra prerequisite clause present on only one side.

| Feat | Claimed Source | Claimed Page | Question | Conflicting Repository Evidence | What Must Be Verified |
|---|---|---|---|---|---|
| **Staggering Attack** | Galaxy at War | 27 | Which prerequisite is correct — a BAB minimum, or a choice of combat feats? | `data/feat-catalog.json` `system.prerequisite`: `"Base Attack Bonus +1"`. `scripts/data/authority/feat-prerequisite-authority.js` (`staggering_attack`): `"Sneak Attack or Rapid Shot or Rapid Strike"`. These are not a paraphrase of each other — they describe entirely different requirements. `prerequisite-checker.js` treats the authority text as the live source of truth (canonical text wins over the catalog field when both exist), so **the authority version ("Sneak Attack or Rapid Shot or Rapid Strike") is what actually gates acquisition today**, not the catalog's BAB text. | Check Galaxy at War p.27 for Staggering Attack's actual prerequisite, and correct whichever of the two repository records is wrong (likely the catalog's `system.prerequisite`, given the authority is what runtime already enforces — but confirm against the book rather than assuming). |
| **Force Training** | Saga Edition Core Rulebook | 94 | Does Force Training require only Force Sensitivity, or also Trained in Use the Force? | `data/feat-catalog.json` `system.prerequisite`: `"Force Sensitivity, Trained in Use the Force."` `FEAT_PREREQUISITE_AUTHORITY` (`force_training`): `"Force Sensitivity"` only. Since authority text wins at runtime, the "Trained in Use the Force" clause the catalog displays is **currently not enforced** during acquisition. | Check Core Rulebook p.94 for whether Force Training requires being Trained in Use the Force in addition to Force Sensitivity. If yes, the authority entry is missing a clause (a real gap, not just descriptive drift). If no, the catalog's extra clause should be removed. |
| **Acrobatic Dodge** | Unknown Regions | 19 | Does Acrobatic Dodge require the Dodge feat, or only Mobility and Skill Focus (Acrobatics)? | `data/feat-catalog.json` `system.prerequisite`: `"Dexterity 13, Dodge, Mobility, Skill Focus (Acrobatics)"`. `FEAT_PREREQUISITE_AUTHORITY` (`acrobatic_dodge`): `"Dexterity 13, Mobility, Skill Focus (Acrobatics)"` — no Dodge. Same runtime consequence as above: the catalog's extra "Dodge" clause is currently not enforced. | Check Unknown Regions p.19 for whether Dodge is a prerequisite of Acrobatic Dodge. |

## Note on sourcebooks unavailable this session

28 feats in the current catalog cite **Legacy Era Campaign Guide** (18) or **Rebellion Era Campaign Guide** (10), neither of which is available in this session (confirmed: no PDF or text for either exists anywhere in this repository or session filesystem). These are already marked `source_unavailable` by `tools/audit-feat-inventory.mjs`'s `sourceConfidence` field — see `docs/audits/generated/feat-inventory-report.json`'s `inventory` array (filter `sourceConfidence: "source_unavailable"`) for the full list. None of them appear in the table above; nothing about their specific claimed text triggered a disagreement finding in this pass. Per this task's brief, they were **not** deleted, downgraded, or otherwise treated as suspect purely for citing an unavailable book — `source_unavailable` records their provenance status, not a validity judgment.

## Regenerating this queue

This queue was hand-triaged, not mechanically generated — `tools/audit-feat-prerequisite-inputs.mjs` produces the full raw `catalog_authority_text_disagreement` list (30 entries as of this writing) that this table was drawn from; re-run it and re-triage if the catalog or authority text changes.
