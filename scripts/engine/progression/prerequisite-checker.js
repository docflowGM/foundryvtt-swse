/**
 * PrerequisiteChecker — Centralized re-export
 *
 * This module re-exports the PrerequisiteChecker from the data layer.
 * Used by capability-registry and other feature-checking systems
 * to validate prerequisites for feats, talents, prestige classes, etc.
 */

export { PrerequisiteChecker } from "/systems/foundryvtt-swse/scripts/data/prerequisite-checker.js";
