import { buildSurveyDefinition } from '../definition-builder.js';

const survey = buildSurveyDefinition({
  surveyId: 'PrC_Independent_Droid',
  surveyType: 'prestige',
  classId: 'independent_droid',
  displayName: 'Independent Droid',
  mentorKey: 'Independent Droid',
});

export default survey;
